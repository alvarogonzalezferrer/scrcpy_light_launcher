# Lazarus compiler-generated binaries (safe to delete)
*.exe
*.dll
*.so
*.dylib
*.lrs
*.res
*.compiled
*.dbg
*.ppu
*.o
*.or
*.a

# Lazarus autogenerated files (duplicated info)
*.rst
*.rsj
*.lrt

# Lazarus local files (user-specific info)
*.lps

# Lazarus backups and unit output folders.
# These can be changed by user in Lazarus/project options.
backup/
*.bak
lib/

# Application bundle for Mac OS
*.app/
/releases_binary/bin
/scrcpy_light_launcher/lib/
/scrcpy_light_launcher/backup
*.o
scrcpy_light_launcher/scrcpy_light_launcher.ini

# Lazarus backup files (Environment Options -> Backup)
*.bak
backup/
*~

# Compiled executables (distribute via Releases, not the repo)
*.exe

# Compiled Lazarus/FPC units and objects
*.o
*.ppu
*.compiled
*.or
*.rsj
lib/

# Lazarus backup files (Environment Options -> Backup)
*.bak
backup/
*~

# Compiled executables (distribute via Releases, not the repo)
*.exe

# Lazarus IDE session state (opcional, mucha gente lo excluye)
*.lps

# Windows junk
Thumbs.db
desktop.ini
*.o
/scrcpy_light_launcher/lib
scrcpy_light_launcher/lib/x86_64-win64/scrcpy_light_launcher.obj
